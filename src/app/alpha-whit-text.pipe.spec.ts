import { AlphaWhitTextPipe } from './alpha-whit-text.pipe';

describe('AlphaWhitTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaWhitTextPipe();
    expect(pipe).toBeTruthy();
  });
});
